# Netlify / Cloudflare Pages SPA fallback
# Будь-який неіснуючий URL під /app — обслуговуємо index.html.
# Решта (включно з / — лендінг) теж віддає index.html, але це й так origin.
/app/*    /index.html   200
/app      /index.html   200
